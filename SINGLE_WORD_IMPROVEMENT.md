# 单字查询功能改进

## 问题描述

用户查询单字（如"你"）时，返回 `null`，提示"未找到词条"。

## 问题原因

粤语词典中通常**不包含单字词条**，只包含常用词汇和短语。因此：
- "你好" ✅ 存在
- "执笠" ✅ 存在
- "你" ❌ 不存在（单字）

这是数据源的设计，不是代码问题。

## 解决方案

当用户查询单字且未找到时，系统会：
1. 查找以该字开头的词语
2. 显示第一个相关词语的发音
3. 在"相关词语"列表中展示所有相关词语

## 功能改进

### 改进前
```
查询: 你
结果: 未找到词条 ❌
```

### 改进后
```
查询: 你
结果:
  词条: 你
  粤拼: nei5
  释义: 该字在词典中无独立词条，以下是包含"你"的词语：
  相关词语:
    - 你位
    - 你们
    - 你副
    - 你去望望
    - 你呢
    - 你哋
    - 你敎
    - 你果
    - 你瞓
    - 你瞧
```

## 代码实现

### 添加的方法

1. `findWordsStartingWith()` - 查找以指定字开头的词语
2. `buildSuggestionDetail()` - 构建建议详情

### 修改的方法

`getWordDetail()` - 增加单字查询的处理逻辑

### 逻辑流程

```
1. 查询用户输入的词
   ↓
2. 未找到
   ↓
3. 判断是否为单字
   ↓
4. 是单字 → 查找以该字开头的词语
   ↓
5. 找到相关词语 → 构建建议详情
   ↓
6. 显示相关词语列表
```

## 测试用例

### 测试1：查询单字
```
输入: 你
预期: 显示以"你"开头的词语列表
```

### 测试2：查询词语
```
输入: 你好
预期: 显示"你好"的完整词条详情
```

### 测试3：查询不存在的词
```
输入: abc
预期: 提示"未找到词条"
```

## 数据验证

### 确认词典中没有单字
```bash
grep -E "^你$" data/word.csv
# 结果：无匹配
```

### 确认词典中有包含"你"的词语
```bash
grep -E "^你" data/word.csv | head -10
# 结果：你位、你们、你副等
```

## 用户体验

### 改进前的问题
- 用户输入单字后看到"未找到词条"
- 不知道该输入什么
- 用户体验差

### 改进后的优势
- 输入单字后能看到相关词语
- 从相关词语中选择想要的词
- 用户体验好

## 注意事项

1. **发音提取**：从第一个相关词语中提取第一个字的发音
   - 例如：从"你好" (nei5 hou2) 中提取 "nei5" 作为"你"的发音

2. **数量限制**：最多显示10个相关词语

3. **性能优化**：使用索引查询，避免全表扫描

## 日志输出

### 单字查询成功的日志
```
[LocalDictionary] ========== getWordDetail 开始 ==========
[LocalDictionary] 获取词条详情: 你
[LocalDictionary] 数据库已就绪，开始查询...
[LocalDictionary] 繁体转换: 你 -> 你 false
[LocalDictionary] 直接查询未找到
[LocalDictionary] 查询单字未找到，尝试查找相关词语
[LocalDictionary] 找到相关词语: 你位, 你們, 你副, ...
[LocalDictionary] buildSuggestionDetail 完成，detail: {...}
[LocalDictionary] ========== getWordDetail 结束 (success with suggestions) ==========
```

### 单字查询失败的日志
```
[LocalDictionary] ========== getWordDetail 开始 ==========
[LocalDictionary] 获取词条详情: 你
[LocalDictionary] 数据库已就绪，开始查询...
[LocalDictionary] 繁体转换: 你 -> 你 false
[LocalDictionary] 直接查询未找到
[LocalDictionary] 查询单字未找到，尝试查找相关词语
[LocalDictionary] ========== getWordDetail 结束 (null, 无相关词语) ==========
```

## 总结

这个改进解决了单字查询的问题，通过显示相关词语来提升用户体验。虽然词典中没有单字词条，但用户仍然可以通过单字找到相关的词语。
