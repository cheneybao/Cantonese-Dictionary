# 粤语词典 - 用户使用指南

## 功能验证结果

根据用户提供的界面截图，以下功能已验证正常：

### ✅ 正常工作的功能
1. **搜索框输入** - 可以正常输入文字
2. **详情查询** - 输入"你好"、"执笠"后可以正确查询到结果
3. **最近搜索** - 可以正确显示历史记录

### ❓ 待验证的功能
1. **联想建议** - 输入过程中的实时联想建议列表（需要用户测试）

## 使用说明

### 方式1：直接搜索完整词语
1. 在搜索框中输入完整词语（如"你好"、"执笠"）
2. 点击搜索按钮或按回车
3. 系统会跳转到词条详情页

**截图已验证**：此功能正常工作 ✅

### 方式2：使用联想建议（待测试）
1. 在搜索框中**慢慢输入**（例如先输入"你"字）
2. **等待1-2秒**（联想功能有300ms延迟）
3. 如果出现联想列表，点击想要的词语即可

**当前状态**：需要用户测试确认

## 常见问题

### Q1: 输入词语后为什么没有联想列表？
**可能原因**：
1. 输入速度太快，还没触发联想延迟
2. 浏览器缓存了旧版本代码
3. 浏览器扩展程序干扰

**解决方法**：
1. 先输入一个字，等待1-2秒
2. 按 `Ctrl+Shift+R` 强制刷新浏览器
3. 检查浏览器控制台是否有错误

### Q2: 如何查看控制台日志？
1. 按 `F12` 打开开发者工具
2. 切换到 "Console" 标签
3. 在搜索框中输入文字
4. 查看日志输出

**应该看到的日志**：
```
[IndexPage] 开始搜索联想: 你
[Network] request 被调用...
[LocalDictionary] getSuggestions 被调用...
```

### Q3: 搜索"执笠"显示什么结果？
**正确结果**：
- 词条：执笠
- 粤拼：zap1 lap1
- 释义：倒闭；收档
- 示例：間舖執笠咗

## 技术信息

### 数据统计
- 总词条数：92717
- 数据版本：1.0
- 数据大小：8.44 MB（压缩后）

### 支持的搜索方式
1. **汉字搜索**：输入汉字或词语
2. **粤拼搜索**：输入粤拼音节（如 "nei5"）
3. **简繁转换**：自动支持简体输入查询繁体词条

### API路由
- `POST /api/dictionary/detail` - 获取词条详情 ✅ 已验证
- `POST /api/dictionary/suggestions` - 获取联想建议 ❓ 待测试
- `POST /api/dictionary/by-jyutping` - 按粤拼音节查询

## 下一步

### 如果联想功能正常
所有核心功能均已验证完成，项目可以正常使用。

### 如果联想功能有问题
请提供：
1. 控制台日志截图
2. 浏览器版本信息
3. 具体的操作步骤

## 调试工具

### 检查词典数据
访问以下URL：
- `/check-indexeddb.html` - 检查IndexedDB中的数据
- `/test-search.html` - 测试搜索功能

### 手动测试命令
```bash
# 测试 detail API
curl -X POST http://localhost:5000/api/dictionary/detail \
  -H "Content-Type: application/json" \
  -d '{"word":"你好"}'

# 测试 suggestions API
curl -X POST http://localhost:5000/api/dictionary/suggestions \
  -H "Content-Type: application/json" \
  -d '{"query":"你"}'
```

## 总结

根据截图显示，**核心的词典查询功能已经正常工作**：
- ✅ 可以搜索"你好"
- ✅ 可以搜索"执笠"
- ✅ 最近搜索功能正常

联想建议功能需要用户进一步测试确认。如果测试发现问题，请提供控制台日志以便进一步排查。
